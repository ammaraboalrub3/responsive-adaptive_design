import 'package:flutter/material.dart';

class ItemIncomeModel {
  final String title, trailing;
  final Color color;

  const ItemIncomeModel(
      {required this.title, required this.trailing, required this.color});
}
